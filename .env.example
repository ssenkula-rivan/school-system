# Database Configuration
# Set to 'True' to use PostgreSQL, 'False' for SQLite (default)
USE_POSTGRES=False

# PostgreSQL Settings (only needed if USE_POSTGRES=True)
DB_NAME=school_management
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432

# Redis Cache Configuration
# Set to 'True' to use Redis, 'False' for local memory cache (default)
USE_REDIS=False
REDIS_URL=redis://127.0.0.1:6379/1

# Django Settings
SECRET_KEY=django-insecure-e1cpt8!-a3!paktt851hlp9rav6#b@3ms@3sby478@^5aolj-w
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Email Configuration
# For REAL email delivery, configure these settings:
# 1. For Gmail:
#    - Enable 2-Factor Authentication on your Gmail account
#    - Generate an "App Password" at https://myaccount.google.com/apppasswords
#    - Use the app password below (not your regular Gmail password)
# 2. Uncomment and fill in the settings below:

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here
DEFAULT_FROM_EMAIL=your-email@gmail.com

# Leave empty to use console backend (emails print to terminal)
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=

